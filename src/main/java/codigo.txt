FUNCAO VAZIA semRetorno <INT a, INT b> (

)


FUNCAO INT fibonacci <INT n> (

    VOLTE 2.
)

FUNCAO INT fatorial <INT n> (

    SE n == 0 (
        VOLTE 1.
    )

    SE n == 1 (
        VOLTE 1.
    )

    VOLTE n * fatorial(n - 1).

)


COMECO
    INT numero.
    REAL real.
    PAL palavra.
    VOUF booleano.

    numero <- 12.

    operadores logicos --> == =/= > < => =< 
    operadores aritmeticos --> * + - :(divisao real) ::(divisao inteira) #(resto)
    operador de concatenação de strings --> & ex a & b 

    SE a > 0 (

    ) DOCONTRARIO SE (

    ) DOCONTRARIO (

    )

    DURANTE i < 1 (

    ) 

    EXIBA(a).

    RECEBA(a).

    EXIBA(`ansdjkdhnasjkdas`)

    fibonacci(2).

FIM